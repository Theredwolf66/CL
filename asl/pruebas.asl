func f(a : float, b : array [10] of int) : float
    var x : int
    var w : float
    return 5+w;
endfunc


func main()
    var p : array [10] of int
    var l : array [1] of bool
    var x,z : float
    var y : bool
    x = f(x,p);
    while y do
        y = true;
    endwhile
    
endfunc
